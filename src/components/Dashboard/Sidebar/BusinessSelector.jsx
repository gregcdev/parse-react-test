var React = require('react');
var PropTypes = React.PropTypes;

var BusinessSelector = React.createClass({

  contextTypes: {
    currentBusiness: React.PropTypes.object
  },

  render: function() {
    return (
      <div />
    );
  }

});

module.exports = BusinessSelector;
